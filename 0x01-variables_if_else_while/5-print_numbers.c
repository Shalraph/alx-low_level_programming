#include <stdio.h>
/**
 *main - Entry point
 *Description: 'Print single digit base ten numbers'
 *Return: Always 0
 */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
